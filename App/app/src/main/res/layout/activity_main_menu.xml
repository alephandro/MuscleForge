<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center">

    <Button
        android:id="@+id/buttonAddTraining"
        android:layout_width="200dp"
        android:layout_height="80dp"
        android:text="Añadir Entrenamiento"
        android:textSize="18sp"
        android:layout_marginBottom="20dp"/>

    <Button
        android:id="@+id/buttonViewTrainings"
        android:layout_width="200dp"
        android:layout_height="80dp"
        android:layout_below="@id/buttonAddTraining"
        android:layout_marginBottom="20dp"
        android:text="Consultar Historial"
        android:textSize="18sp" />

    <Button
        android:id="@+id/buttonDeleteTraining"
        android:layout_width="200dp"
        android:layout_height="80dp"
        android:text="Eliminar Entrenamiento"
        android:textSize="18sp"
        android:layout_below="@id/buttonViewTrainings"/>

</RelativeLayout>
