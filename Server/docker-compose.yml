version: '3'

services:
  app:
    image: backend:v2
    ports:
      - "8888:8888"
    networks:
      - server-net
    volumes:
      - db_data:/var/lib/mysql
    depends_on:
      - db

  db:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: MuscleForge
      MYSQL_USER: rana
      MYSQL_PASSWORD: rana
    ports:
      - "3306:3306"
    networks:
      - server-net
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:

networks:
  server-net:
